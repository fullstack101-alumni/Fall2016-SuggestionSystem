/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { SuggestionboxaubgApiService } from './suggestionboxaubg-api.service';

describe('Service: SuggestionboxaubgApi', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SuggestionboxaubgApiService]
    });
  });

  it('should ...', inject([SuggestionboxaubgApiService], (service: SuggestionboxaubgApiService) => {
    expect(service).toBeTruthy();
  }));
});
